#ifndef TYPES_H
#define TYPES_H

enum class ObjectType  : char { Red, PokojeKolumny, salaTronowa, ScianySufitPodloga, Okna, Doors };
enum class ShaderType  : char { Standard, Standard2 };
enum class TextureType : char { Purple, Red, White, Wood };

#endif
